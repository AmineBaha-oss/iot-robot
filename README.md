# IoT Smart Mobile Robot (Raspberry Pi)

Telemetry (IR, ultrasonic, camera status) → Adafruit IO.  
Local daily CSV logging and repo structure for submission.

> Code lives under `src/`, configs in `config/`, logs/data excluded from git.
